{
  "buildCommand": "bundle install && jekyll build",
  "outputDirectory": "_site",
  "framework": "jekyll",
  "installCommand": "gem install bundler:2.4.22 && bundle install",
  "build": {
    "env": {
      "RUBY_VERSION": "3.3.0"
    }
  }
}
