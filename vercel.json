{
  "buildCommand": "bundle install && jekyll build",
  "outputDirectory": "_site",
  "framework": "jekyll",
  "installCommand": "gem install bundler:2.2.33 && bundle install",
  "build": {
    "env": {
      "RUBY_VERSION": "3.0.2"
    }
  }
}
