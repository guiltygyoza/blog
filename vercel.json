{
  "buildCommand": "bundle install && jekyll build",
  "outputDirectory": "_site",
  "framework": "jekyll",
  "installCommand": "gem install bundler:1.17.2 && bundle install",
  "build": {
    "env": {
      "RUBY_VERSION": "2.6.3"
    }
  }
}
